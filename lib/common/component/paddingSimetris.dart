import 'package:flutter/material.dart';

class PaddingSimetris extends StatelessWidget {
  const PaddingSimetris({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}